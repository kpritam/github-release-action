name: Coursier

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PGDATA: /var/lib/postgresql/13/main
    steps:
      - uses: actions/checkout@master

      - name: Install PostgreSQL
        run: echo "/usr/lib/postgresql/13/bin" >> $GITHUB_PATH

      - run: sudo ls -la /var/lib/postgresql/13/main
      - run: sudo ls -la /var/lib/pgsql/data
      - run: sudo postgres --hba_file=pg_hba.conf --unix_socket_directories=/tmp
          
          
          
